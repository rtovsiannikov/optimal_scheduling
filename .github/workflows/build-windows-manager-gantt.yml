name: Build Windows desktop app with manager Gantt

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - "patch_manual_data_input.py"
      - "patch_manager_gantt.py"
      - ".github/workflows/build-windows-manager-gantt.yml"
      - "desktop_app/**"
      - "cp_sat_scheduler.py"
      - "FactoryScheduler.spec"
      - "run_desktop_app.py"
      - "requirements.txt"
      - "requirements_desktop.txt"
      - "generated_factory_demo_data/**"

jobs:
  build-windows-manager-gantt:
    runs-on: windows-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Upgrade pip
        run: python -m pip install --upgrade pip

      - name: Install dependencies
        run: |
          python -m pip install -r requirements.txt
          python -m pip install -r requirements_desktop.txt
          python -m pip install pyinstaller

      - name: Apply manual data-input patch if present
        shell: pwsh
        run: |
          if (Test-Path patch_manual_data_input.py) {
            python patch_manual_data_input.py
          } else {
            Write-Host "patch_manual_data_input.py not found; skipping manual-input patch."
          }

      - name: Apply manager Gantt patch
        run: python patch_manager_gantt.py

      - name: Build Windows application
        run: pyinstaller FactoryScheduler.spec

      - name: Create ZIP archive
        shell: pwsh
        run: |
          Compress-Archive -Path dist\FactoryScheduler\* -DestinationPath FactoryScheduler-Windows-ManagerGantt.zip -Force

      - name: Upload Windows artifact
        uses: actions/upload-artifact@v4
        with:
          name: FactoryScheduler-Windows-ManagerGantt
          path: FactoryScheduler-Windows-ManagerGantt.zip
